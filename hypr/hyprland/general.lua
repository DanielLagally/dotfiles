require("_upstream.hyprland.general")

hl.config({
    dwindle = {
        smart_split = false,
        force_split = 2,
        preserve_split = true,
    },
    render = {
        direct_scanout = 2,
    },
})
